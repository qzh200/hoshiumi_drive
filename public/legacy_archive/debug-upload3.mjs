const base = 'http://127.0.0.1:8788';
let cookie = '';
const r0 = await fetch(base + '/api/auth/login', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ key: 'NmyBWykw2jsQVZmG5vs6' }) });
cookie = r0.headers.getSetCookie().map((c) => c.split(';')[0]).join('; ');
const form = new FormData();
form.append('path', 'root-probe.txt');
form.append('file', new Blob(['probe'], { type: 'text/plain' }), 'root-probe.txt');
const up = await fetch(base + '/api/files', { method: 'POST', headers: { Cookie: cookie }, body: form });
const text = await up.text();
console.log('status', up.status);
const lines = text.split('\n').filter((l) => /(Error|error|at |TypeError|ReferenceError|undefined|\.js)/.test(l));
console.log(lines.slice(0, 40).join('\n'));
console.log('--- tail ---');
console.log(text.slice(-800));
